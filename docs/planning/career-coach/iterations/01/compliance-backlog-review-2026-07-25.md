# Compliance Backlog Review — Career Coach, 2026-07-25

> 📦 **Ported from `singularity` on 2026-09-08.** Content is unchanged; the only edits are this
> banner and the links that could not survive the move, which are **marked in place rather than
> redirected** — see the corpus README.
> Part of the Career Coach corpus — see [the corpus README](../../README.md).
> 🔴 Links outside this corpus resolve only in a `singularity` checkout and are **not ported**.
> Provenance: [ADR-002](../../../../architecture/adr-002-lean-agile-os-is-the-upstream-platform.md).

Promised in Sprint 3's Retrospective ("batch review the accumulating low-severity compliance
backlog before Sprint 5/6"), deferred through Sprint 4 and 5, actioned here as Sprint 5's
Retrospective action item 1 — before Sprint 6 implementation work starts.

## Items reviewed

| Item                                                                        | Decision                                                                     | Reasoning                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ATDD Scenario-title "I CAN"/"I CANNOT" naming (Sprints 2-5)                 | **Fixed**                                                                    | Purely mechanical — renamed all 31 `Scenario:` titles across all 8 career-coach `.feature` files (`build-my-strategy-plan`, `source-positions`, `qualify-positions`, `give-feedback-refine-requirements`, `apply-to-positions` in `domain-e2e`; `strategy-plan-api`, `position-api`, `application-api` in `api-e2e`). Titles don't affect Cucumber step matching, so this carried zero regression risk — confirmed by re-running both suites (71/71, 103/103 Scenarios still green).                                                                                                        |
| Repo-wide `/* eslint-disable */` scaffold noise (Sprint 1)                  | **Already resolved, not career-coach's item**                                | The parking-lot entry's own scope is a _repo-wide_ sweep across other projects (job-search, freelance-portal, prompt-workbench, agents) — `grep -rl eslint-disable apps/career-coach libs/career-coach` returns zero matches today, so career-coach's own instance was already cleaned up. The remaining scope belongs to those other projects, not this backlog.                                                                                                                                                                                                                           |
| Hardcoded dev-fixture auth (`CURRENT_USER_ID`/`DEV_USER_ID`, Sprint 1)      | **Deferred, unchanged**                                                      | Genuinely blocked on the phase that puts `career-coach-api` behind `api-gateway`/`platform-iam` — not a quick fix, a real architecture dependency. Restated, not re-decided.                                                                                                                                                                                                                                                                                                                                                                                                                |
| Missing DOM-HTTP-Domain UI Cucumber test (Sprint 1)                         | **Deferred, unchanged**                                                      | Same blocker as above — no live-API path exists for career-coach's UI yet (no gateway wiring, no docker-compose service). Restated, not re-decided.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `positionId`/`applicationId` path params have no validation pipe (Sprint 3) | **Deferred, explicit decision (not silently re-accepted)**                   | These are internal-format IDs (`POS-XXXX`, `APP-<uuid>`), not user-supplied structured data (e.g. no `ParseIntPipe` equivalent applies). The repository lookup itself already rejects an unknown ID (`No Position/Application found with id ...`, thrown and surfaced as an error response) — a missing format-validation pipe doesn't create a real gap beyond what every other `:id` path param in this controller family already has (`qualify`, `feedback`, `pursue` all share this shape). Consistent, low severity; not worth a bespoke regex pipe for an internal identifier format. |
| Commit-message format (`Sprint N Tasks X-Y` vs. ADR-015's `Phase N Item M`) | **Accepted as career-coach's own established convention, explicit decision** | Used consistently since Sprint 1 across all 5 Sprints so far — rewriting history to match ADR-015 retroactively isn't worth the churn/risk (would require rebasing already-merged, already-PR'd commits). Going forward, new commits keep the `Sprint N Task(s) X-Y` shape career-coach has used throughout; this is a project-local convention divergence from ADR-015, not an oversight.                                                                                                                                                                                                  |

## Outcome

One real fix landed (ATDD naming, 31 Scenarios). Four items re-affirmed as deliberate,
already-reasoned deferrals — recorded explicitly here so they don't need re-litigating every
Sprint's compliance pass, and so a future session can find this review rather than re-discovering
the same items from scratch. One item found to be already resolved and out of scope.
